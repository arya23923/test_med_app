export const API_URL = window.location.hostname === "localhost" ? "http://localhost:8181" : "http://localhost:8181";
// export const API_URL = <add your server side url></add>
console.log(
    "API_URL :",
    API_URL
);